type Procedure {
  _id: ID!
  title: String!
  procedureId: String
  type: String
  period: Int
  title: String
  currentStatus: String
  abstract: String
  tags: [String]
}

type Query {
  getProcedure(_id: ID!): Procedure
  getProcedures(pageSize: Int, offset: Int): [Procedure]
}
