type Procedure {
  _id: ID!
  title: String!
  procedureId: String
  type: String
  period: Int
  currentStatus: String
  abstract: String
  tags: [String]
}

type Query {
  getProcedure(_id: ID!): Procedure
  procedures(pageSize: Int, offset: Int): [Procedure]
}
